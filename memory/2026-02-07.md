# 2026-02-07

## Double-Cal (F:\openClaw\Double-Cal)
- User confirmed:
  - Enable transfer even when source hasn't pressed '=' yet, by silently evaluating expression (do not mutate source state).
  - Allow leading-dot decimals like ".5".
  - Project location: F:\openClaw\Double-Cal.

## Work done today
- Initialized Nuxt project in F:\openClaw\Double-Cal (started from nuxi minimal template; corrected to Nuxt 3.x).
- Installed deps:
  - nuxt 3.x, pinia, @pinia/nuxt
  - vitest + @vitest/coverage-v8
- Updated config/scripts:
  - nuxt.config.ts: added @pinia/nuxt module and global CSS assets/styles.css
  - package.json: added scripts "test" and "test:watch"
- Implemented core code files (engine + stores + UI skeleton):
  - utils: guards.ts, format.ts, parser.ts (tokenize + shunting-yard to RPN), evaluate.ts
  - stores: settings.ts, calc.ts (input/backspace/clear/evaluate + transfer with overwrite/append; silent eval for transfer)
  - composables: useKeyboard.ts
  - UI: app/app.vue (theme), pages/index.vue, components: TwinCalcLayout, CalculatorPanel, Display, Keypad (long-press ⌫ + optional haptics), TransferControl, SettingsDrawer

## Still needed (continue tomorrow)
- Create assets/styles.css with full mobile-first styling + dark theme variables + responsive portrait/landscape layout.
- Add missing buttons/keys validation (ensure keypad includes all required keys; check minus label uses '−' but emits '-').
- Add Vitest config (vitest.config.ts) and implement tests:
  - tests/parser.spec.ts
  - tests/evaluate.spec.ts
  - tests/transfer.spec.ts
- Ensure Nuxt/Pinia integration works end-to-end (run dev, run tests).
- Fix any parsing edge cases (e.g., empty expression handling, trailing operators, parentheses mismatch) if tests reveal issues.
- Git: commit current work in F:\openClaw\Double-Cal once tests pass.

## Notes / pitfalls observed
- nuxi init + git initialization step was slow/hung; avoid relying on it. Manual git init is safer.
- Codex CLI flags: passing prompt via stdin works best in PowerShell (Get-Content -Raw file | codex ...). Codex exec subcommand parsing is finicky.