# 2026-02-08

## Double-Cal (F:\openClaw\Double-Cal)
- Added full mobile-first styling in `assets/styles.css` with light/dark theme via `.app[data-theme]`, safe-area insets, focus-visible rings, and responsive portrait/landscape layout.
- Fixed Nuxt build failure by updating root `tsconfig.json` to extend `./.nuxt/tsconfig.json` (previous Nuxt4-style references caused missing `.nuxt/tsconfig.app.json`).
- Verified `npm run build` succeeds.
- Initialized git history, renamed branch to `main`, added remote `https://github.com/Capybarajack/-Double-Cal.git`, pushed `main`.
