# 2026-02-14

- Implemented 3-tier memory system:
  - Hot memory: `MEMORY.md` ≤200 lines with [P0]/[P1]/[P2] + TTL rules ([P1]=90d, [P2]=30d).
  - Cold memory: expired/pruned items archived under `memory/archive/` (kept searchable by OpenClaw `memory_search`).
  - Raw logs: `memory/YYYY-MM-DD.md` remain untouched and are not auto-loaded.
- Added `memory-janitor.py` script to auto-archive expired items and prune oldest P2 items if needed to keep `MEMORY.md` ≤200 lines; warns if P1/P2 missing `[ts:...]` (won’t auto-archive those).
- Created OpenClaw cron job `memory-janitor (daily archive)` (jobId: 2d101883-a572-41db-9099-968a1cf47e94) and updated schedule to run daily at 08:39 Asia/Taipei.
- Packaged the above memory setup as a new skill `openclaw-memory-janitor` and pushed to CapyOpenCLAW repo under `skills/openclaw-memory-janitor/`.
