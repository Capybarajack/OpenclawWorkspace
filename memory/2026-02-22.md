# 2026-02-22

## LINEsticker 專案進度快照（給下次續做）

- 專案路徑：`F:\openClaw\LINEsticker`
- 已完成：Phase 0 scaffold + commit `bc50782`
- team-tasks：
  - `line-sticker-phase0` 已完成（4/4）
  - `line-sticker-phase1` 已初始化，階段任務已分配

### 目前未提交變更（Phase 1 草稿）
- `.env.example`
- `.gitignore`
- `README.md`
- `docker-compose.dev.yml`
- `services/api/package.json`
- `services/api/src/server.ts`
- `services/rembg-service/app.py`
- `services/rembg-service/requirements.txt`
- `services/worker/package.json`
- `services/worker/src/worker.ts`

### 草稿內容方向
- API：upload / packs / generate / pack status / stickers / SSE events / internal callback
- Worker：BullMQ consume `generate-pack`，呼叫 rembg `/remove-bg`，用 sharp 輸出 370x320 PNG
- rembg-service：`/remove-bg` 改為真實 remove() 去背
- compose：加入 api/worker 服務

### 續作結果（晚間）
- 已完成 Phase 1 提交：`8e011a6`
- 主要成果：
  - API：`/api/v1/uploads/reference`、`/api/v1/packs`、`/api/v1/packs/:packId/generate`、`/api/v1/packs/:packId`、`/api/v1/packs/:packId/stickers`、`/api/v1/jobs/:jobId/events`、內部 callback endpoint。
  - Worker：BullMQ consume `generate-pack`，呼叫 rembg，使用 sharp 輸出 370x320 PNG 到 `data/outputs/<packId>/<seq>.png`。
  - rembg-service：`/remove-bg` 已改為真實 `rembg.remove()`。
  - compose：新增 api/worker/rembg/redis 可本機一鍵啟動。
- 驗證：
  - `npm --workspace services/api run build` ✅
  - `npm --workspace services/worker run build` ✅
  - `py -m py_compile services/rembg-service/app.py` ✅
  - `docker compose -f docker-compose.dev.yml config` ✅

### 續作結果（晚間第二輪）
- 已完成 Phase 2 提交：`3942003`
- 主要成果：
  - 新增單張重試 API：`POST /api/v1/packs/:packId/items/:seq/retry`
  - 新增 ZIP 匯出流程：
    - `POST /api/v1/packs/:packId/export`
    - `GET /api/v1/exports/:exportId`
    - `GET /api/v1/exports/:exportId/download`
  - 新增內部 callback：`POST /api/v1/internal/exports/:exportId/status`
  - worker 支援 `export-zip` job，輸出 `data/exports/<packId>/<exportId>.zip`
  - ZIP 內容含 `/stickers/*.png` 與 `manifest.json`
- 驗證：
  - `npm --workspace services/api run build` ✅
  - `npm --workspace services/worker run build` ✅

### 續作結果（晚間第三輪 / Phase 3 進行中）
- 目前分支狀態：`F:\openClaw\LINEsticker` 有未提交前端變更（尚未 commit）。
- 已新增前端模組（Wizard + API 串接草稿）：
  - `apps/mobile-web/src/components/StatusBadge.vue`
  - `apps/mobile-web/src/components/StepGeneration.vue`
  - `apps/mobile-web/src/components/StepPackConfig.vue`
  - `apps/mobile-web/src/components/StepResultActions.vue`
  - `apps/mobile-web/src/components/StepUploadReference.vue`
  - `apps/mobile-web/src/components/StickerItemList.vue`
  - `apps/mobile-web/src/components/WizardStepper.vue`
  - `apps/mobile-web/src/composables/useStickerWorkflow.ts`
  - `apps/mobile-web/src/lib/apiClient.ts`
  - `apps/mobile-web/src/styles.css`
  - `apps/mobile-web/src/vite-env.d.ts`
- 既有檔案修改：
  - `apps/mobile-web/src/App.vue`
  - `apps/mobile-web/src/main.ts`
  - `README.md`
- 目標功能（草稿已含）：
  - 上傳、建立 pack、啟動 generate、SSE + polling fallback、單張 retry、export 建立與輪詢、下載連結顯示。

### 下次起手建議（接續 Phase 3）
1. 先跑 `npm --workspace apps/mobile-web run build` 確認前端可編譯。
2. 起服務做 smoke test：上傳 → 生成 → 單張重試 → 匯出 ZIP。
3. 修正前端型別/流程細節後 commit（建議訊息：`feat(phase3): add mobile wizard frontend integration`）。
4. 再進 Phase 4（TTL 清理、retry backoff、端到端驗收）。
