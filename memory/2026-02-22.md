# 2026-02-22

## LINEsticker 專案進度快照（給下次續做）

- 專案路徑：`F:\openClaw\LINEsticker`
- 已完成：Phase 0 scaffold + commit `bc50782`
- team-tasks：
  - `line-sticker-phase0` 已完成（4/4）
  - `line-sticker-phase1` 已初始化，階段任務已分配

### 目前未提交變更（Phase 1 草稿）
- `.env.example`
- `.gitignore`
- `README.md`
- `docker-compose.dev.yml`
- `services/api/package.json`
- `services/api/src/server.ts`
- `services/rembg-service/app.py`
- `services/rembg-service/requirements.txt`
- `services/worker/package.json`
- `services/worker/src/worker.ts`

### 草稿內容方向
- API：upload / packs / generate / pack status / stickers / SSE events / internal callback
- Worker：BullMQ consume `generate-pack`，呼叫 rembg `/remove-bg`，用 sharp 輸出 370x320 PNG
- rembg-service：`/remove-bg` 改為真實 remove() 去背
- compose：加入 api/worker 服務

### 下次起手建議
1. 先在 `F:\openClaw\LINEsticker` 跑 `npm install`
2. `npm --workspace services/api run build` 與 `npm --workspace services/worker run build`
3. `docker compose -f docker-compose.dev.yml up -d --build` 做 smoke test
4. 驗證 API/worker/rembg 流程後再 commit Phase 1
