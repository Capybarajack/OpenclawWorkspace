# 2026-02-23

## LINEsticker 續作進度

- 專案路徑：`F:\openClaw\LINEsticker`
- 本次完成：Phase 3（前端 wizard 串接）
- 專案 commit：`79aad1f`
  - `feat(phase3): add mobile wizard frontend integration`

### 主要完成內容
- 新增前端結構：
  - `apps/mobile-web/src/components/*`（Upload/Config/Generation/Result/Stepper/Status）
  - `apps/mobile-web/src/composables/useStickerWorkflow.ts`
  - `apps/mobile-web/src/lib/apiClient.ts`
  - `apps/mobile-web/src/styles.css`
  - `apps/mobile-web/src/vite-env.d.ts`
- 串接 API：
  - upload / create pack / generate
  - pack + stickers 查詢
  - SSE 進度流（斷線 fallback 到 3 秒 polling）
  - 單張 retry
  - export 建立 + 2 秒輪詢 + ready 後 download
- README 已補 Frontend Phase 3 使用方式

### 驗證
- `npm --workspace apps/mobile-web run build` ✅

### team-tasks
- `line-sticker-phase3` 已完成（4/4 done）

### 續作結果（Phase 4 完成）
- 專案 commit：`151d2aa`
  - `feat(phase4): add export TTL cleanup and retry guard/backoff`
- 主要成果：
  - API retry 限制：`RETRY_MAX_ATTEMPTS`（預設 2）
  - API retry backoff：`RETRY_BACKOFF_MS`（預設 3000），過快重試回 `429` + `retryAfterMs`
  - Export TTL：`ZIP_TTL_HOURS`（預設 24），過期查詢/下載回 `410`
  - API 定期清理過期 ZIP（5 分鐘掃描）
  - README / `.env.example` 已更新
- 驗證：
  - `npm --workspace services/api run build` ✅
  - `npm --workspace services/worker run build` ✅
  - `npm --workspace apps/mobile-web run build` ✅
- 受限項目：Docker 引擎未啟動（`dockerDesktopLinuxEngine` pipe not found），完整 docker e2e smoke 暫未執行。

### 追加（無 Docker 本機模式）
- 專案 commit：`96a35f6`
  - `chore(dev): add no-docker local launcher scripts for Windows`
- 新增：
  - `scripts/dev-local.ps1`（一鍵啟動 rembg/api/worker/web，可用旗標跳過服務）
  - `scripts/dev-local-stop.ps1`（依 PID 記錄安全停止）
  - README 新增「No Docker (Local Dev on Windows)」教學
- 追加修正 commit：`8689b69`
  - `dev-local.ps1` 支援 Python 啟動器 fallback（`python` -> `py`）
  - `-NoRemBg` 時完整跳過 Python 依賴檢查
- 驗證：PowerShell parser check 通過（兩支 ps1）；`dev-local.ps1 -NoRemBg` 可成功拉起 api/worker/web。

### 續作結果（雙模式 queue 進行中）
- 目標：加入 `QUEUE_MODE=redis|local`，讓本機無 Redis 也可跑 queue。
- 目前狀態：尚未提交（WIP）
- 未提交檔案：
  - `.env.example`
  - `README.md`
  - `scripts/dev-local.ps1`
  - `services/api/src/server.ts`
  - `services/worker/src/worker.ts`
- 備註：`scripts/.dev-local-pids.json` 是執行腳本產生的暫存，不應提交。

### 下次起手建議
1. 先跑 build 驗證：
   - `npm --workspace services/api run build`
   - `npm --workspace services/worker run build`
   - `npm --workspace apps/mobile-web run build`
2. 啟動 `QUEUE_MODE=local` 做 smoke（上傳→生成→重試→ZIP）。
3. 確認 `QUEUE_MODE=redis` 相容後再 commit。
4. 補跑 e2e smoke（若需要再開 Redis / Docker）。
