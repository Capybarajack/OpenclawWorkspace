# 2026-02-24

## LINEsticker 雙模式 queue 完成

- 專案路徑：`F:\openClaw\LINEsticker`
- 專案 commit：`f60cd30`
  - `feat(queue): add dual queue mode (redis/local) for API and worker`

### 本次完成內容
- API 新增 queue adapter（`QUEUE_MODE=redis|local`）
  - `redis`：維持 BullMQ
  - `local`：寫入 `data/local-queue/<queueName>/pending/*.json`
- Worker 新增 local queue polling 模式
  - 輪詢 `pending` → 原子 rename 到 `processing`
  - 成功後刪除 processing 檔
  - 失敗移至 `failed`
- `dev-local.ps1`：無 Docker 流程預設 `QUEUE_MODE=local`
- `.env.example`：新增 `QUEUE_MODE`、`LOCAL_QUEUE_POLL_MS`
- README：補 queue mode 說明與 no-docker 對應用法

### 驗證
- `npm --workspace services/api run build` ✅
- `npm --workspace services/worker run build` ✅
- `npm --workspace apps/mobile-web run build` ✅
- smoke（`QUEUE_MODE=local`, `-NoRemBg`）
  - API/worker/web 可啟動 ✅
  - upload/create/generate 可 enqueue + worker 消化 job ✅
  - 因 NoRemBg，item 最終 `failed(fetch failed)` 屬預期 ✅

### 追加進度（晚間）
- 目標：補 rembg 可用環境。
- 已做：
  - `services/rembg-service/requirements.txt` 更新為 `rembg==2.0.72`、`pillow==12.1.0`。
- 目前狀態：依賴尚未完整安裝（`py -c "import fastapi,..."` 仍顯示 `ModuleNotFoundError: fastapi`）。
- 專案工作樹目前未提交變更：
  - `services/rembg-service/requirements.txt`

### 下次建議
1. 先完成 `py -m pip install -r services/rembg-service/requirements.txt`。
2. 驗證 `py -c "import fastapi, uvicorn, rembg, PIL, multipart"`。
3. 再跑 `dev-local.ps1`（不加 `-NoRemBg`）做完整鏈路測試。
